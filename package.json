{
  "name": "hide",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "run-p start-compile start-run",
    "dev-serve": "run-p start-compile start-serve",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w",
    "start-serve": "browser-sync start --server -w --host 0.0.0.0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/p5": "^1.7.6",
    "browser-sync": "^3.0.4",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "bbst": "^0.2.1"
  }
}
